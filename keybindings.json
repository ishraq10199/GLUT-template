// (vscode) CTRL+SHIFT+P > KEYBOARD SHORTCUTS(JSON) > COPY & PASTE FOLLOWING
//
// [Warning] This keybind change is applied globally
//
// Place your key bindings in this file to override the defaults
[
  {
    "key": "ctrl+b",
    "command": "workbench.action.terminal.sendSequence",
    // Following line should be commented on Windows
    "args": { "text": "\u0003 make\n" }

    // Following line should be commented on Linux
    // "args": { "text": "\u0003 mingw32-make.exe\n" }
  }
]
